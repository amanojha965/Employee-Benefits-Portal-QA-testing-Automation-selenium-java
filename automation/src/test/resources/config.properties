# Base URL of the running frontend (see README for how to serve it locally).
baseUrl=http://localhost:5500

# Browser to launch: chrome (only chrome is wired up in DriverFactory for now).
browser=chrome

# Explicit wait timeout, in seconds, used across all Page Objects.
explicitWaitSeconds=10

# Run Chrome without a visible window (true/false).
headless=false
